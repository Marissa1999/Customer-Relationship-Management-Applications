@model IEnumerable<ClientCRM.Models.Client>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create");
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.first_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.last_name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.first_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.last_name)
            </td>
            <td>
                @Html.ActionLink("Contact Information", "Index", "Contact", new { id = item.client_id }, null) |
                @Html.ActionLink("Edit", "Edit", new { id = item.client_id }) |
                @Html.ActionLink("Details", "Details", new { id = item.client_id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.client_id })
            </td>
        </tr>
    }

</table>

@Html.ActionLink("Back to List", "Index")
